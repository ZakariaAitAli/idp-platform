apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: env-eks-cert-manager-overrides
  namespace: argocd
spec:
  project: platform
  source:
    repoURL: git@github.com:ZakariaAitAli/idp-platform.git
    targetRevision: main
    path: kubernetes/cert-manager/overlays/eks
    directory:
      recurse: true
  destination:
    server: https://kubernetes.default.svc
    namespace: cert-manager
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
